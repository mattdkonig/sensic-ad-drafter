import { describe, it, expect, vi } from 'vitest';
import { syncBibles } from '../lib/sync-worker.mjs';

describe('syncBibles parser', () => {
  it('should be tested via the local script', async () => {
    expect(true).toBe(true);
  });
});
